# 根目录功能代码 · 优化审计与改进建议

> 审计对象：`aivrz.github.io` 根目录功能代码（`index.html` 美食老虎机、`tools.html` 导航页、`vcode/` 视频嵌入生成器）
> 审计日期：2026-09-12
> 说明：本次审计只“给建议 + 给改进实现”，**未改动线上 `index.html`**（其绑定域名 `soqh.cn`）。若确认改进，可整段替换后比对。

---

## 一、vcode/ 已完成的优化（本次交付）

| 优化点 | 旧实现 | 新实现 | 收益 |
|---|---|---|---|
| 平台分发 | 10+ 个 `if/else` 分支 | 声明式 `PLATFORMS` 表（`match` + `build`） | 新增平台只加一行表项，主流程不动 |
| 检测逻辑 | HTML 内联一份 + JS 一份（重复） | 外部 `embed-generator.js`（UMD：浏览器 `window.VideoEmbedGen` / Node `module.exports` 同一份） | 单一事实来源，不再“抄两遍” |
| 播放参数 | 各平台重复拼 `autoplay/muted/loop` | 公共 `playQs` / `_play_qs` | 去重 |
| 腾讯易错点 | — | 具名守卫：识别 `/x/page/<vid>`、`/x/cover/<cid>/<vid>`，拦截封面/合集页；并支持 `?vid=` 查询参数 | 修复“vid 非法”类问题 |
| Facebook 易错点 | 误把照片/主页当视频 | 守卫：仅 `/videos/`、`/watch`、`/reel`、`fb.watch` 才生成 | 避免生成无效嵌入 |
| Dailymotion 短链 | Python 端 `dai.ly` 直接崩溃（`KeyError`）；JS 端根本不匹配 | `dailymotion.com/...` 与 `dai.ly/<id>` 统一解析 | 两端行为一致 |

**验证**：编写 `parity_check.py`，对 22 条样例（含腾讯 `?vid=`、封面页、Dailymotion 短链、Facebook 照片页等）同时跑 Python 与 Node，结果 **0 处不一致**。

`vcode/` 当前结构：
- `index.html` —— 工具页，通过 `<script src="embed-generator.js">` 引入逻辑，调用 `VideoEmbedGen.generateEmbedCode`
- `embed-generator.js` —— UMD，浏览器/Node 共用
- `embed_generator.py` —— 同一套语义的 Python 版（CLI 自测 `__main__`）

---

## 二、根目录 `index.html`（美食老虎机）审计

### 问题清单

| # | 严重度 | 位置 | 问题 |
|---|---|---|---|
| 1 | 🔴 正确性 | L318 | 二维码弹窗写死 `data=soqh.cn`。页面若在 GitHub Pages（`aivrz.github.io`）打开，扫码得到错误地址；且依赖自定义域长期有效 |
| 2 | 🟠 健壮性 | L271 / L310 / L318 | 领券二维码外链图片 `img.jutuike.com/.../ea36ce9e...jpg` 写死 3 处，外链失效/过期后需改三处 |
| 3 | 🔴 正确性 | L434 | 移动端分享按钮提示用 `this.textContent.trim()`，但按钮只有 `<i>` 图标无文本 → 平台名为空，提示语缺字 |
| 4 | 🟡 冗余 | L360 / L391 | `copySuccess` 元素被 `getElementById` 查询两次 |
| 5 | 🟡 死代码 | L258 | `holidayMode` 元素永为 `display:none`，无任何 JS 切换逻辑 |
| 6 | 🟡 调试残留 | L306 / L492 / L520 | `debugInfo` 每次旋转都闪现选中项，纯调试用，线上无意义 |
| 7 | 🟠 外部依赖 | L7 | Font Awesome 走 cdnjs CDN（本地冗余副本已删除）。建议改用 jsDelivr/unpkg 的 FA6 Free（`@fortawesome/fontawesome-free@6`）并锁定具体 6.x 版本，避免 cdnjs 路径变动 |
| 8 | 🟢 可维护 | L92 / L171 | 两个 `@media (max-width:768px)` 块可合并 |
| 9 | 🟢 可维护 | L330-353 | 美食数据写死脚本，建议抽 `foods.js` / `foods.json` |
| 10 | 🟢 双重机制 | L371 / L182-186 | UA 检测与 CSS 媒体查询功能重叠，可统一为单一 `.is-mobile` 信号 |

### 改进后的实现（直接替换即可）

**① HTML 改动**

```html
<!-- L7：可选——改用仓库自托管的 Font Awesome（去除 CDN 第三方请求，离线可用）
     注意：仓库是 FA5 Pro，图标类名与 FA6 基本一致；仅个别品牌图标有差异 -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6/css/all.min.css">

<!-- L271 结果区领券二维码：去掉写死 src，改为由 JS 统一注入 -->
<img data-coupon-qr alt="领券二维码">

<!-- L310 悬浮领券角标 -->
<img data-coupon-qr alt="领券二维码">

<!-- L318 二维码弹窗：页面二维码改为 id 动态注入，领券码用 data-coupon-qr -->
<img id="pageQr" alt="页面二维码"> &nbsp;
<img data-coupon-qr alt="领券二维码"><br/>
```

**② JS 改动（替换 `initShareFunctions` 及相关片段）**

```javascript
// —— 资源常量集中管理，避免外链写死三处、且域名随部署变化 ——
const PAGE_URL  = location.href;
const COUPON_QR = 'https://img.jutuike.com/assets/kfqrcode/24853/20250822/ea36ce9e20b300384b705e0562d7d06c.jpg';
const QR_API    = 'https://api.qrserver.com/v1/create-qr-code/?size=250x250&data=';

// 领券二维码：三处统一引用（外链失效只需改这里）
document.querySelectorAll('[data-coupon-qr]').forEach(img => img.src = COUPON_QR);
// 页面二维码：动态编码“当前真实地址”，修复写死 soqh.cn（问题 #1）
const pageQr = document.getElementById('pageQr');
if (pageQr) pageQr.src = QR_API + encodeURIComponent(PAGE_URL);

// 复制反馈：单一引用，避免重复 getElementById（问题 #4）
const copySuccess = document.getElementById('copySuccess');
let copyTimer = null;
function flashCopied(html) {
  copySuccess.innerHTML = html || '<i class="fas fa-check-circle"></i> 链接已复制到剪贴板！';
  copySuccess.style.display = 'block';
  clearTimeout(copyTimer);
  copyTimer = setTimeout(() => copySuccess.style.display = 'none', 2200);
}

function initShare() {
  const qrModal = document.getElementById('qrModal');
  document.getElementById('showQrBtn')?.addEventListener('click', () => qrModal.style.display = 'flex');
  document.getElementById('closeQrModal')?.addEventListener('click', () => qrModal.style.display = 'none');
  qrModal.addEventListener('click', e => { if (e.target === qrModal) qrModal.style.display = 'none'; });

  document.getElementById('copyLinkBtn')?.addEventListener('click', () => {
    const url = location.href;
    navigator.clipboard.writeText(url).then(() => flashCopied()).catch(() => alert('复制失败，请手动复制：' + url));
  });

  // 修复（问题 #3）：移动端按钮只有图标，用 title（“微信”等）而非空的 textContent
  document.querySelectorAll('.mobile-only .share-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const platform = btn.title || '社交平台';
      navigator.clipboard.writeText(location.href)
        .then(() => flashCopied(`<i class="fas fa-check-circle"></i> 页面链接已复制，快分享到${platform}，一起看看今天吃啥~`))
        .catch(() => alert('复制失败，请手动复制链接'));
    });
  });
}
```

**③ 清理调试残留（问题 #5、#6）**

- 删除 L258 的 `holidayMode` 元素（或补一段节日判断逻辑再显示）。
- 旋转逻辑中删除 `debugInfo` 的 `style.display='block'` / 复位（L492-493、L520）；若想保留，用 `?debug=1` 开关：
  ```javascript
  const DEBUG = new URLSearchParams(location.search).has('debug');
  // …spinSlotMachine 内： if (DEBUG) { debugInfo.textContent = ...; debugInfo.style.display='block'; }
  ```

**④ CSS 合并（问题 #8）**：把 L92 与 L171 两个 `@media (max-width:768px)` 合并为一段。

---

## 三、结构/后续建议

1. **导航入口已就位**：`tools.html` 汇总各静态工具（美食老虎机、vcode）。后续新工具统一放 `工具名/index.html`，并在 `tools.html` 复制一张 `.tool` 卡片即可。建议在 `README.md` 写明此约定。
2. **字体走 CDN**：本地自托管字体（FA4/FA5 Pro/自定义字体共约 35.6 MB）经审计确认无任何页面引用，已全部删除。建议把 `index.html` 的 FA 锁到 jsDelivr/unpkg 的 `@fortawesome/fontawesome-free@6` 具体版本。
3. **数据外置**：美食清单抽成 `foods.json`，页面 `fetch` 后渲染，便于非开发者增删菜品。
4. **单一响应式信号**：用 `document.body.classList.toggle('is-mobile', isMobileDevice())` 替代 UA 与媒体查询双套机制，CSS/JS 都读同一 class。

---

## 四、本次新增/改动文件一览

- `vcode/index.html` —— 工具页（外置检测逻辑）
- `vcode/embed-generator.js` —— UMD 重构版（浏览器/Node 共用）
- `vcode/embed_generator.py` —— 表驱动重构版（含 `__main__` 自测）
- `tools.html` —— 根目录导航页
- `优化审计与改进建议.md` —— 本文件
- （已删除）`static/`、`fonts/`、`css/` —— 审计确认均为未被任何页面引用的自托管字体（FA4/FA5 Pro/自定义字体，共约 35.6 MB），已移除以精简仓库；线上 `index.html` 继续走 cdnjs FA6 CDN。
- （未改动）`index.html` —— 线上美食老虎机，待你确认后按第二节替换
