# 美食老虎机（今天吃啥）优化方案 & 商业化分析

> 分析对象：`aivrz.github.io/index.html`（美食老虎机，绑定域名 `soqh.cn`，托管于 GitHub Pages，纯静态）
> 目标：面向“选择困难症”做体验优化；出结果弹窗含领券/小程序二维码 + 分享；扩展玩法（骰子/大转盘）；支持增删与自定义菜品；给出商业化路径。
> 说明：本方案**已落地**——`aivrz.github.io/index.html` 已整段替换为实现版（老虎机 / 大转盘 / 骰子三模式 + 分类筛选 + 自定义菜品 + 结果弹窗）。本文件保留为设计依据与商业化路线图。

## 零、落地状态（2026-09-12）

| 项 | 状态 | 备注 |
|---|---|---|
| 三模式切换（🎰老虎机 / 🎡大转盘 / 🎲骰子） | ✅ 已实现 | 共享 `allDishes()` 数据源与筛选条件 |
| 分类/场景筛选 chips | ✅ 已实现 | 从菜品 `type` 去重生成，选中后三模式共同过滤 |
| 自定义菜品（localStorage） | ✅ 已实现 | 键 `my_dishes_v1`，可增删，🍴 标记，仅本机生效 |
| 结果弹窗（领券码 + 分享） | ✅ 已实现 | 领券码用常量 `COUPON_QR` 管理；分享优先 `navigator.share`，回退复制+页面二维码 |
| 页面二维码动态域名 | ✅ 已修复 | 用 `location.href` 经 `api.qrserver.com` 生成，不再写死 `soqh.cn` |
| **小程序码** | ⚠️ 占位 | 常量 `MINIAPP_QR` 当前为空 → 弹窗自动隐藏该区块；需预先生成小程序码图放到 `static/qr/miniapp.png` 后填入 |
| 死代码清理 | ✅ 已完成 | 删除 `holidayMode`/`debugInfo`/双套分享按钮/悬浮角标等 |
| 扩充 `dishes.json`（站长级） | ⬜ 未做 | 当前仍内联 `DEFAULT_DISHES`；后续可按 3.3 抽离 |
| 单选/近期去重 | ⬜ 未做 | P1，可后续补 |

---

## 一、现状速览（基于现有代码）

| 模块 | 现状 | 评价 |
|---|---|---|
| 旋转机制 | 3 个滚轮，CSS `top` 过渡，随机停在一个菜品 | 可用，但“随机”对选择困难症帮助有限（没有缩小范围的能力） |
| 结果展示 | 旋转后在 `#resultContainer` 内联显示 emoji+菜名，并常驻一张领券二维码 | 领券码是外链写死图（`img.jutuike.com/...`）；分享入口分散在移动端/桌面端两套按钮 |
| 分享 | 移动端微信/QQ/微博/抖音图标（复制链接）+ 桌面端“二维码/复制链接”弹窗 | 弹窗二维码写死 `soqh.cn`（`优化审计与改进建议.md` 已指出此 bug）；移动端提示取不到平台名 |
| 菜品数据 | `foodItems` 数组**写死 22 条**，含 name/type/color/emoji | 非数据驱动；访客无法增删，站长改菜要改源码 |
| 死代码 | `holidayMode`、`debugInfo` 两个元素永不生效/仅调试 | 应清理 |

**结论**：当前是“能转、能出结果”的 MVP。对选择困难症的核心痛点（*我知道大概想吃啥类型，但定不下来*）缺少“缩小范围”的能力；商业化（领券）已有雏形但入口弱、且依赖易失效的外链。

---

## 二、针对“选择困难症”的核心优化

1. **分类/场景筛选（最关键）**：出结果前先选「类型」或「场景」，只在子集里转。
   - 类型芯片：大餐 / 快餐 / 面食 / 小吃 / 轻食 / 日料 / 西餐 …
   - 场景模式：一个人吃 / 聚餐 / 夜宵 / 减肥日 / 预算<30 → 预设过滤
   - 价值：把“22 选 1”变成“5 选 1”，选择困难症最买账。
2. **“换一个 / 就这个”**：结果出来后给两个按钮——`换一个`（排除当前结果重抽）、`就吃这个`（确认并写入今日记录）。
3. **避免短期重复**：用 `localStorage` 记住最近 N 次结果，提供“别再推这个”开关，或自动降低近期菜品的权重。
4. **结果弹窗替代内联**：旋转结束直接弹窗（见第三节），焦点更集中，二维码+分享一步到位。
5. **轻量陪伴感**：结果附一句随机“推荐语”（如“今天值得犒劳自己一顿火锅 🔥”），缓解决策焦虑。

---

## 三、功能方案详述（含代码片段）

### 3.1 结果弹窗：领券/小程序二维码 + 分享
把现有内联结果 + 分散的分享按钮，合并为**一次弹窗**。要点：
- 弹窗显示大号菜品 emoji+名称+推荐语
- 展示**小程序码**（领券/今天吃啥小程序）——静态站无法运行时生成微信小程序码，需**预先生成图片**放到仓库 `static/qr/miniapp.png` 或外链，作为可配置常量 `MINIAPP_QR`
- 展示**领券二维码** `COUPON_QR`（现有 jutuike 图，建议改为常量或自托管）
- 分享按钮：优先 `navigator.share`（移动端系统分享面板），不支持则回退“复制链接 + 页面二维码”
- 页面二维码用 `location.href` 动态生成（修复 `soqh.cn` 写死 bug）

```html
<!-- 结果弹窗（替换原 #resultContainer + #qrModal） -->
<div class="result-modal" id="resultModal">
  <div class="result-card">
    <button class="rm-close" id="rmClose">×</button>
    <div class="rm-emoji" id="rmEmoji">🍲</div>
    <div class="rm-name" id="rmName">火锅</div>
    <div class="rm-tip" id="rmTip">今天值得犒劳自己一顿！</div>
    <div class="rm-actions">
      <button id="rmAgain">换一个</button>
      <button id="rmOk">就吃这个</button>
    </div>
    <div class="rm-qr">
      <div><img id="rmMiniQr" alt="小程序码"><div>扫码用小程序</div></div>
      <div><img id="rmCouponQr" alt="领券二维码"><div>扫码领券更划算</div></div>
    </div>
    <div class="rm-share">
      <button id="rmShare">分享给朋友</button>
      <img id="rmPageQr" alt="页面二维码">
    </div>
  </div>
</div>
```

```javascript
// 旋转结束回调里调用 showResult(selectedFood)
const MINIAPP_QR  = 'static/qr/miniapp.png';           // 预先生成的小程序码
const COUPON_QR   = 'https://img.jutuike.com/.../ea36ce9e....jpg'; // 领券码（建议自托管）
const QR_API      = 'https://api.qrserver.com/v1/create-qr-code/?size=240x240&data=';

function showResult(food){
  document.getElementById('rmEmoji').textContent = food.emoji;
  document.getElementById('rmName').textContent = food.name;
  document.getElementById('rmTip').textContent  = pickTip(food);
  document.getElementById('rmMiniQr').src   = MINIAPP_QR;
  document.getElementById('rmCouponQr').src = COUPON_QR;
  document.getElementById('rmPageQr').src   = QR_API + encodeURIComponent(location.href);
  document.getElementById('resultModal').classList.add('show');
}
// 分享：优先系统分享，回退复制+二维码
document.getElementById('rmShare').addEventListener('click', async () => {
  const data = { title:'今天吃啥', text:`不如吃 ${currentFood.name}？`, url: location.href };
  if (navigator.share) { try { await navigator.share(data); } catch(e){} }
  else { navigator.clipboard.writeText(location.href); alert('链接已复制，去粘贴给朋友吧'); }
});
```

### 3.2 自定义菜品（访客级，localStorage）
选择困难症用户常想加“我家附近的私房菜”。纯静态无后端也能做——用 `localStorage` 持久化，按浏览器保存：
```javascript
const KEY = 'my_dishes';
function loadCustom(){ try { return JSON.parse(localStorage.getItem(KEY)) || []; } catch { return []; } }
function saveCustom(list){ localStorage.setItem(KEY, JSON.stringify(list)); }
function allDishes(){ return DEFAULT_DISHES.concat(loadCustom()); } // 合并默认+自定义

// 添加 UI：输入框 + “添加”按钮
function addDish(name){
  const list = loadCustom();
  list.push({ name, type:'自定义', color:'#7f8c8d', emoji: pickEmoji() });
  saveCustom(list); renderDishes(); // 重新渲染滚轮/转盘/骰子数据源
}
// 管理：列出自定义菜品，可单独删除（仅删本地，不影响默认）
```
- 进阶：支持 **导入/导出 JSON**（“备份我的菜单”），多设备间手动迁移。
- 注意：localStorage 仅本机本浏览器生效，符合“静态、无后端”前提；不做跨用户同步。

### 3.3 扩充更多菜品（站长级）
- 把 `foodItems` 抽成 `dishes.json`，页面 `fetch('./dishes.json')` 加载（GitHub Pages 支持同域 fetch，https 下正常）。
- 站长改 JSON 即可上架新菜，无需动逻辑；访客自定义叠加在其上。
- 给每道菜加 `price`、`tags`（如 `辣`/`清淡`/`适合聚餐`）字段，支撑更细筛选与“预算<30”场景。

### 3.4 更多玩法（均为纯前端，无后端）
- **大转盘（推荐首选）**：用 `conic-gradient` 画扇区 + 旋转动画，落点算法取随机角对应扇区。比老虎机更“轻、快、喜庆”，移动端体验好。
  ```javascript
  function spinWheel(dishes){
    const seg = 360 / dishes.length;
    const land = Math.floor(Math.random()*dishes.length);
    const rot = 360*5 + (360 - land*seg - seg/2); // 多转几圈后停在目标扇区
    wheel.style.transform = `rotate(${rot}deg)`;
    // transitionend 后取 dishes[land]
  }
  ```
- **投骰子**：掷 1~2 颗骰子。可作“吃不吃外卖”的 yes/no，或把 6 个面映射到 6 个类型再在类型内随机——趣味性强、实现极简。
- **翻牌/抽签**：一排背面卡片，翻一张即结果；或用「抽签桶」动画。
- **模式切换**：顶部 Tab（`老虎机 / 大转盘 / 骰子`），共享同一份 `allDishes()` 数据源与筛选条件，工作量可控。

### 3.5 筛选器（与玩法共用）
- 从 `allDishes()` 提取 `type` 去重渲染芯片；选中后旋转/转盘/骰子只在子集里取。
- 场景预设 = 预置的 `type` 集合 + 预算上限，一键套用。

---

## 四、静态实现的关键约束与对策（GitHub Pages）

| 需求 | 静态站能否做 | 对策 |
|---|---|---|
| 访客自定义菜品 | ✅ | `localStorage`（本机持久化），导入/导出 JSON 做跨设备 |
| 分享 | ✅ | `navigator.share` → 回退复制链接 + 动态页面二维码 |
| 二维码 | ✅ | 页面/分享用 `api.qrserver.com` 动态生成（或自托管）；**小程序码必须预先生成图片**（微信小程序码需后端 access_token，纯静态无法实时生成） |
| 领券/小程序导流 | ✅ | 展示**预先备好的二维码图片**（外链或仓库 `static/qr/`） |
| 跨用户数据/排行榜 | ❌（需后端） | 暂不做；或接 Serverless（如 Cloudflare Workers）做匿名统计，超出“纯静态”范畴时再上 |
| 直接投放商业广告 | ⚠️ | GitHub Pages 条款对页面直接商业广告较敏感，**建议变现走小程序/公众号/外链**，Pages 只承载工具与引流 |

---

## 五、商业化方案

现有 `img.jutuike.com` 领券码本质已是 **CPS 返利**（用户扫码领外卖/淘宝券并下单，站长拿佣金）。据此放大：

1. **结果弹窗高意图转化（P0）**：用户刚“决定吃啥”时情绪最高、转化最好，把领券/小程序码放进弹窗（而非角落常驻），点击率显著提升。
2. **小程序矩阵（P0）**：把“今天吃啥”做成微信小程序，弹窗放**小程序码**导流安装；小程序内可接：
   - 激励视频 / 插屏广告（合规、收益稳）
   - 优惠券 CPS（外卖/电商联盟）
   - 会员（去广告、专属玩法、自定义菜单云同步）
3. **公众号/私域（P1）**：弹窗放公众号二维码，沉淀用户做社群运营；团购、本地商家联合推广。
4. **多工具互导流量（已具备）**：`tools.html` 把美食老虎机、vcode 等串成**小工具矩阵**，互相导流，提升整体曝光与广告/小程序安装基数。
5. **品牌域名 `soqh.cn`（已具备）**：强化“今天吃啥”品牌；可挂“买域名联系”变现（现有页脚已留邮箱），亦可做域名停靠/跳转联盟。
6. **合规变现提示**：GitHub Pages 上**避免直接挂商业广告**（条款风险），变现统一收口到小程序 / 公众号 / 外链券；Pages 本身作为免费工具与流量入口。可考虑打赏（微信赞赏码 / 爱发电）作为合规的补充。
7. **数据资产（P2）**：匿名统计各菜品被选中热度（`localStorage` 汇总或可选上报自托管统计如 Plausible/Umami），反哺“热门推荐”与商家合作谈判筹码。

---

## 六、实施路线图（优先级）

- **P0（体验+变现立刻见效）**：结果弹窗（领券/小程序码+分享，修复 `soqh.cn` 与移动端提示 bug）+ 分类/场景筛选 + 清理死代码。
- **P1（粘性）**：自定义菜品（localStorage）+ 扩充默认菜品为 `dishes.json` + “换一个/就吃这个” + 近期去重。
- **P2（趣味+矩阵）**：大转盘 / 骰子模式切换 + 推荐语。
- **P3（商业化闭环）**：小程序码/公众号码导流 + 自托管统计 + 多工具矩阵联动。

---

## 七、与既有审计修复的衔接
- `优化审计与改进建议.md` 已指出并给出修复：二维码写死 `soqh.cn`、领券图外链重复 3 处、移动端分享取空平台名、调试残留。本方案第 3.1 节的弹窗实现已一并修复这些问题（动态 `location.href` 页面码、常量管理领券码、用 `navigator.share`/`title` 取平台名）。
- 字体清理已完成（仓库 35.7MB→99.8KB），弹窗/转盘新增样式可继续沿用系统字体栈，不引入新依赖。
